apiVersion: 1

providers:
- name: 'teslamate'
  orgId: 1
  folder: ${DASHBOARD_FOLDER:-Teslamate}
  folderUid: ${DASHBOARD_FOLDER_UID:-Nr4ofiDZk}
  type: file
  disableDeletion: false
  editable: true
  updateIntervalSeconds: ${DASHBOARD_POLL_INTERVAL:-10}
  options:
    path: /var/lib/grafana/dashboards